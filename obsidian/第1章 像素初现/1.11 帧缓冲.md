# 1.11 帧缓冲

**初始状态**：谜题 1.10 的渲染通道程序。

**目标状态**：为每个 image view 创建对应的 `VkFramebuffer`。程序退出前全部销毁。

**规则**：
- 每个 framebuffer 的 `renderPass = render_pass_`，`width/height = swapchain_extent_`
- `attachmentCount = 1`，`pAttachments` 指向对应的 image view
- 不需要 depth-stencil attachment

---

← [[章节总览]]
