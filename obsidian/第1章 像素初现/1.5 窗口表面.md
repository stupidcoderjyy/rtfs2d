# 1.5 窗口表面

**初始状态**：谜题 1.4 的调试信使程序。

**目标状态**：成功创建 `VkSurfaceKHR`，在程序退出前正确销毁。

**规则**：
- 使用 `glfwCreateWindowSurface(instance, window, nullptr, &surface)` 创建表面
- surface 必须在 instance 之前销毁：`vkDestroySurfaceKHR(instance, surface, nullptr)`
- 不需要校验 surface 格式——那是后面的事

---

← [[章节总览]]
